services:
  cache:
    image: redis
    expose:
      - 6379
  app:
    image: tilt.dev/express-redis-app
    depends_on:
      - cache
    ports:
      - 3000:3000
    environment:
      - REDIS_URL=redis://cache
      - NODE_ENV=development
      - PORT=3000
